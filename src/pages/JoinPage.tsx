import PlaceholderPage from "../components/PlaceholderPage";

function JoinPage() {
  return (
    <PlaceholderPage
      eyebrow="Player"
      title="Join a game"
      description="Enter a room code (or scan the host's QR code) to jump into a live trivia game. Coming in the next development phase."
    />
  );
}

export default JoinPage;
